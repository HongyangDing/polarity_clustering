# Changning Plot

## 项目说明

这个仓库实现了一条从地震目录和连续波形出发，到事件特征构建、层次聚类和结果可视化的完整流程。当前主线只有 4 个步骤：

1. `sel_temp.py`：从总目录中筛选模板事件，生成 `temp.pha` 和 `detected.pha`
2. `cut_waveforms.py`：按台站、分量、相位切出模板事件和待分类事件的波形矩阵
3. `cc_cal.py`：由互相关构造事件特征矩阵 `combined_RR_all_2col.npy`
4. `cluster.py`：对事件特征矩阵做层次聚类，并输出图件、文本结果和 HTML

当前默认算法尽量贴近 Shelly (2016) 的主思路：

- 事件之间的相对极性信息来自波形互相关
- `cc_cal.py` 会在允许时滞窗内同时使用主峰和次峰信息构造相对极性测量
- 由于当前流程没有 matched-filter 生成的模板-检测事件对应关系，默认会把低于阈值的测量置零，作为 Shelly 工作流的代理近似
- 每个观测块使用 SVD 第一左奇异向量构造事件特征
- `cluster.py` 使用 cosine distance 和层次聚类

需要特别说明的是：

- Step 0 的模板筛选是当前工程为了构建模板目录加入的预处理，不是 Shelly (2016) 固定给出的标准步骤
- Step 2 的 `|CC|` 阈值和 Step 3 的 linkage 方法，都是当前工程的实现参数，不是论文里在本仓库中已被唯一确认的固定值
- Step 3 默认切树方式已按 Shelly (2016) 收到 `maxclust=100`

## 当前文件结构

- `config.py`
  主配置文件。所有程序行为默认值都在这里定义，并按 Step 0 到 Step 3 分区。
- `sel_temp.py`
  从总目录中筛选模板事件，并输出 `temp.pha` 和 `detected.pha`。
- `cut_waveforms.py`
  用 MPI 并行切波形矩阵。
- `cc_cal.py`
  计算 Step 2 事件特征矩阵 `combined_RR_all_2col.npy`。
- `cluster.py`
  执行聚类并输出图件、摘要、标签和 HTML。
- `lib/cut.py`
  提供台站文件读取、目录解析、任务展开和波形切片函数。
- `lib/corr.py`
  提供批量归一化互相关函数。
- `submit_step0_select.sh`
  Step 0 的 Slurm 提交脚本。
- `submit_step1_cut_waveforms.sh`
  Step 1 的 Slurm 提交脚本。
- `submit_step2_cc_cal_gpu.sh`
  Step 2 的 Slurm 提交脚本。
- `submit_step3_cluster.sh`
  Step 3 的 Slurm 提交脚本。
- `input/`
  输入目录，当前使用 `xue.sta`、`temp.pha`、`detected.pha`。
- `shelly.pdf`
  参考论文。

## 输入数据格式

### 台站文件

`config.fsta` 默认指向 `./input/xue.sta`。

每一行格式：

```text
NET.STA,lat,lon,elevation
```

### 目录文件

`config.source_catalog` 是总目录文件，只在 Step 0 使用。  
`config.ftemp` 和 `config.fdetected` 分别表示模板目录和待分类目录。

目录文件的当前解析规则是：

- 事件头行以 `20` 开头
- 事件头行字段顺序为 `origin_time,lat,lon,dep,mag,event_id`
- 随后的拾取行至少需要包含 `net_sta,tp,ts,...`
- `tp` 或 `ts` 缺失时用 `-1`

### 波形目录

`config.data_path` 指向波形根目录。当前代码假定单日单台单分量文件路径为：

```text
<data_path>/<YYYYMMDD>/<NET.STA>.<YYYYMMDD>.<CHANNEL>.mseed
```

例如：

```text
/path/to/data/20240101/SC.XYZ.20240101.BHZ.mseed
```

## 配置规则

当前主线约定非常简单：

- 程序行为和算法默认值统一放在 `config.py`
- `submit_step*.sh` 只负责 Slurm 资源申请、并行方式和运行环境
- 提交脚本里不应该出现会改变阈值、模式、切树方式或输出逻辑的设置
- 当前提交脚本按 `freeosc_202502.pdf` 的 freeosc 使用说明编写
- CPU 步骤显式使用 `-p cpu`，GPU 步骤显式使用 `-p gpu`
- 所有提交脚本统一使用 `--get-user-env`，把登录节点上准备好的 modules / conda 环境带到计算节点

### Step 0 配置

- `fsta`
  台站文件路径
- `ftemp`
  模板目录路径
- `fdetected`
  待分类目录路径
- `source_catalog`
  总目录路径
- `template_min_magnitude`
  模板事件最小震级
- `template_min_station_count`
  模板事件最少台站数

### Step 1 配置

- `data_path`
  波形根目录
- `output_detected`
  待分类事件波形矩阵输出目录
- `output_temp`
  模板事件波形矩阵输出目录
- `channel_list`
  参与计算的分量列表
- `phase_ids`
  参与计算的相位编号列表
- `P_t_before`、`P_t_after`
  P 相窗口前后长度
- `S_t_before`、`S_t_after`
  S 相窗口前后长度
- `t_before`、`t_after`
  供代码按 `phase_id` 访问的窗口列表
- `t_before_for_filter`、`t_after_for_filter`
  滤波前扩展窗口
- `freq_min`、`freq_max`
  带通滤波频率范围
- `sampling_rate`
  目标采样率

### Step 2 配置

- `step2_shelly2016_abs_cc_min`
  主峰 `|CC|` 阈值；低于该值的测量会被置零
- `step2_shelly2016_min_peak_sep_sec`
  主峰和次峰允许被视为两个不同峰值时的最小时间间隔
- `step2_lag_max_sec`
  P/S 相相关搜索窗最大时滞
- `step2_template_batch_size`
  一次送入 GPU 的模板批大小
- `step2_enable_cudnn_benchmark`
  CUDA 下是否开启 `cudnn.benchmark`
- `step2_feature_matrix_out`
  Step 2 输出文件名

### Step 3 配置

- `step3_matplotlib_backend`
  Matplotlib backend；`None` 表示自动选择
- `step3_mplconfig_dir`
  Matplotlib 缓存目录
- `step3_linkage_method`
  层次聚类 linkage 方法
- `step3_cluster_cut_mode`
  `distance` 或 `maxclust`；当前默认是 `maxclust`
- `step3_distance_cut_threshold`
  `distance` 模式使用的距离阈值
- `step3_maxclust_n_clusters`
  `maxclust` 模式使用的目标簇数；当前默认是 `100`
- `step3_top_n_show`
  图件和 HTML 中重点展示的最大簇数
- `step3_show_noise`
  是否在静态图中显示无效行/零向量行
- `step3_save_fig`
  是否保存静态图
- `step3_fig_out`
  静态图输出路径
- `step3_export_html`
  是否导出 HTML
- `step3_html_out`
  HTML 输出路径

## 运行流程

### Step 0：筛模板目录

```bash
python sel_temp.py
```

或：

```bash
sbatch submit_step0_select.sh
```

这一步会读取 `config.source_catalog`，按震级和台站数筛模板，并写出：

- `config.ftemp`
- `config.fdetected`

如果你已经有现成的 `temp.pha` 和 `detected.pha`，可以直接跳过这一步。

### Step 1：切波形矩阵

```bash
python cut_waveforms.py
```

或：

```bash
sbatch submit_step1_cut_waveforms.sh
```

这一步会针对每个 `台站 × 分量 × 相位` 组合分别生成矩阵文件：

- `output_detected/<sta>_<channel>_<phase>.npy`
- `output_temp/<sta>_<channel>_<phase>.npy`

矩阵形状为：

```text
(事件数, 时间采样点数)
```

矩阵的行顺序与目录文件中的事件顺序一致。

### Step 2：构建事件特征矩阵

```bash
python cc_cal.py
```

或：

```bash
sbatch submit_step2_cc_cal_gpu.sh
```

这一步会逐个读取每个 `台站 × 分量 × 相位` 的模板矩阵和待分类矩阵，构造 `detected × template` 的相对极性测量矩阵，并把每个观测块的第一左奇异向量拼接成最终事件特征矩阵：

- `combined_RR_all_2col.npy`

该矩阵的形状为：

```text
(待分类事件数, 台站-分量-相位组合数)
```

当前 `submit_step2_cc_cal_gpu.sh` 默认申请 `--gres=gpu:a100:1` 以优先运行速度；如果你更看重排队成功率，只需要改这一行 GPU 资源申请，不需要改任何程序参数。

### Step 3：聚类与可视化

```bash
python cluster.py
```

或：

```bash
sbatch submit_step3_cluster.sh
```

这一步会读取 Step 2 的事件特征矩阵，去掉无效行或零向量行，计算 cosine distance，执行层次聚类，并输出：

- `cluster_labels.npy`
- `cluster_valid_mask.npy`
- `cluster_summary.txt`
- `cluster_distance_matrix.txt`
- `cluster_map_profiles.png`
- `detected_3d_slider_by_cluster.html`

## 各文件函数说明

### `sel_temp.py`

- `select_template_event_ids(event_dic, min_magnitude, min_station_count)`
  根据震级和台站数筛选模板事件
- `write_template_and_detected_catalogs(event_lines, template_ids, template_out, detected_out)`
  按原始目录行顺序写出模板目录和待分类目录
- `main()`
  Step 0 主入口

### `cut_waveforms.py`

- `prepare_jobs()`
  根据台站表、分量和相位展开全部切片任务
- `cut_catalog_waveforms(dataset_name, catalog_path, output_dir, jobs_for_rank, rank)`
  对单个目录执行切波形并保存矩阵
- `main()`
  Step 1 主入口，使用 MPI 分发任务

### `cc_cal.py`

- `slice_correlation_lag_window(corr_mat, pha_id, fs)`
  截取允许的时滞搜索窗
- `get_shelly2016_relative_polarity(corr_mat, pha_id, fs, min_sep_sec=None)`
  根据主峰和次峰构造 Shelly 风格相对极性测量
- `apply_cc_threshold(r, abs1, cc_min=None)`
  用主峰 `|CC|` 阈值筛选相对极性测量
- `choose_device(rank)`
  根据当前运行环境选择 `cuda`、`mps` 或 `cpu`
- `load_job_list()`
  生成全部 `台站 × 分量 × 相位` 任务
- `first_left_singular_vector(r_k)`
  对单个观测块做 SVD，并返回第一左奇异向量
- `build_feature_column(temp_mat, detected_mat, pha_id, fs)`
  计算单个观测块对应的事件特征列
- `main()`
  Step 2 主入口，使用 MPI 汇总全部观测块结果

### `cluster.py`

- `get_cut_plan(n_valid)`
  根据当前切树模式返回 `fcluster` 所需参数
- `load_feature_matrix(path)`
  读取 Step 2 输出的事件特征矩阵
- `sanitize_feature_matrix(X)`
  去掉非有限值和零向量行
- `relabel_by_size(labels)`
  按簇大小重新编号
- `build_cluster_order(labels)`
  构造簇编号、簇成员索引和簇大小
- `cosine_distance(a, b)`
  计算两个向量之间的余弦距离
- `cluster_medoid_info(X_proc_full, labels, all_event_ids, cluster_ids)`
  为每个簇选出代表向量和代表事件编号
- `hierarchical_cluster_features(X)`
  Step 3 的主聚类函数
- `save_cluster_summary(out_file, cluster_ids, cluster_sizes, medoid_eids)`
  保存簇摘要
- `save_cluster_distance_matrix(out_file, shown_cluster_ids, medoid_vecs)`
  保存展示簇之间的距离矩阵
- `get_event_xyz_from_dic(all_dic, event_ids)`
  取事件经纬度和深度
- `collect_event_arrays(all_dic, event_ids)`
  取事件时间、位置、深度和震级
- `lonlat_to_local_km(lon, lat)`
  把经纬度转成局部千米坐标
- `build_cluster_palette(cluster_ids)`
  为展示簇建立颜色表
- `_mag_scale(mag)`
  根据震级缩放散点大小
- `mag_to_plotly_size(mag, base_size)`
  生成 Plotly 点大小数组
- `export_html_plotly_slider_by_cluster(...)`
  导出 HTML 时间滑条
- `plot_map_and_profiles(...)`
  绘制平面图和两个剖面图
- `main()`
  Step 3 主入口

### `lib/cut.py`

- `read_fsta(fsta)`
  读取台站文件
- `read_fpha_list(fpha)`
  读取目录文件并解析成事件字典
- `read_pha_simplified(fpha)`
  读取目录原始文本，供 Step 0 重写目录使用
- `iter_event_waveform_jobs(sta_names)`
  展开 `台站 × 分量 × 相位` 任务
- `build_waveform_groups(event_dic, event_ids, data_path, sta_name, channel, phase_id)`
  按 `waveform_path` 对事件分组，并保留每个事件原始行号
- `load_waveforms(data_path, event_dic, event_ids, sta_name="XX.C038", channel="BHZ", phase_id=1)`
  读取单个观测块的二维波形矩阵

### `lib/corr.py`

- `correlate_conv1d_matrix(data1, temp, mode="full")`
  计算批量归一化互相关

## 当前实现特点

### 事件行号一致性

- Step 1 中，`load_waveforms()` 虽然会先按 `waveform_path` 分组以减少重复读盘，但最终仍通过原始 `row index` 写回矩阵
- Step 2 中，会先压缩掉全零波形行再计算，但会把结果按布尔掩码散回原始 detected 行
- Step 3 中，`cluster_labels.npy` 的长度始终与 `detected.pha` 的事件数一致；未参与聚类的行标签为 0

### Step 1 计算方式

- 每个 `台站 × 分量 × 相位` 组合生成一个矩阵文件
- 波形切片前会执行去均值、去趋势、taper 和带通滤波
- 当前实现按 `waveform_path` 分组读取，因此单个 job 内同一个日波形文件通常只读一次

### Step 2 计算方式

- 每个观测块构造一个 `detected × template` 测量矩阵
- 当前实现会先剔除全零模板行和全零待分类行，再做相关和 SVD
- 当前 Step 2 统一采用一条 Shelly 风格主线：主峰、次峰、阈值筛选再进入 SVD

### Step 3 计算方式

- 聚类基于事件特征向量的 cosine distance
- 聚类标签会按簇大小重新编号
- 无效行和零向量行不会参与聚类，最终记为 0

## 推荐运行顺序

```bash
sbatch submit_step0_select.sh
sbatch submit_step1_cut_waveforms.sh
sbatch submit_step2_cc_cal_gpu.sh
sbatch submit_step3_cluster.sh
```

如果你已经有现成的 `temp.pha`、`detected.pha` 或中间矩阵文件，可以从对应步骤直接开始。

## 运行环境

当前主线依赖的主要 Python 包：

- `numpy`
- `scipy`
- `matplotlib`
- `obspy`
- `mpi4py`
- `torch`
- `plotly`

建议在 `obspy` conda 环境中运行。

如果你在 freeosc 上运行，建议先在登录节点准备好环境，再执行 `sbatch`。例如：

```bash
conda activate obspy
sbatch submit_step1_cut_waveforms.sh
```

如果 Step 2 需要集群 CUDA module，则先在登录节点执行对应的 `module load cuda/<version>`，再提交 GPU 作业。提交脚本中的 `--get-user-env` 会把该环境带到计算节点。
